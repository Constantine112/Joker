import types from '../types/request'
import { myAxios, IP } from '../../../../utils/interaction.js'

const state = {
    userId: undefined,
}

const getters = {

}

const actions = {
    [types.actions.getTypingOrder]: (context, data) => {
        return
    }
}